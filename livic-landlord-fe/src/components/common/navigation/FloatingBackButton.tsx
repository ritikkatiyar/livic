import React from 'react';

export default function FloatingBackButton({ onPress }: { onPress?: () => void }) {
  // Mobile devices feature native edge-swipe and system back navigation by default.
  // Floating back buttons are disabled to prevent duplicate chrome and clutter.
  return null;
}
